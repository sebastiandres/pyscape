---
title: "Habitación 7:"
include-in-header:
  text: |
    <script type="module">
    window.GLOBAL_URL = "https://raw.githubusercontent.com/sebastiandres/pyscape/refs/heads/main/quarto/rooms/template.py";
    </script>
    <style>
    #quarto-sidebar, #quarto-sidebar-container { display: none !important; }
    </style>
---

Se han detectado objetos cercanos a la estación espacial, y se deben ingresar los datos al escaner para detectar que resultan un peligro o no.

Crea una lista ordenada por las letras teniendo de referencia desde Z hastaA con los objetos que sean grandes.

<div class="alert alert-info">
Ordenar ocupando la función `sorted(lista)` donde `lista` es la variable que contiene el listado de objetos.
</div>

::: {.callout-tip title="Pista" collapse="true"}
Pista de la habitación

```python 
respuesta=True
```
:::

```{pyodide-python}
objetos = ["MEDIANO_3", "MEDIANO_1", "MEDIANO_3", "GRANDE_1", "PEQUEÑO_10", "GRANDE_10", "GRANDE_3", "PEQUEÑO_1", "PEQUEÑO_10"]

# Escribe tu respuesta aquí
respuesta = None
print(respuesta)

# Verifica la respuesta
import sala
sala.revisar(respuesta)
```